import 'package:flutter/material.dart';

class QuizCredential extends StatefulWidget {
  const QuizCredential({Key? key}) : super(key: key);

  @override
  State<QuizCredential> createState() => _QuizCredentialState();
}

class _QuizCredentialState extends State<QuizCredential> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
